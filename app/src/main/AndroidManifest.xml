<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.raktavahini.app">

    <application
        android:allowBackup="true"
        android:label="RaktaVahini"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <activity android:name=".DonationLogActivity" />
        <activity android:name=".DonorProfileActivity" />
        <activity android:name=".SearchBloodActivity" />
        <activity android:name=".RegisterDonorActivity" />

        <activity
            android:name=".MainActivity"
            android:exported="true">   <!-- ✅ ADD THIS -->

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

    </application>

</manifest>