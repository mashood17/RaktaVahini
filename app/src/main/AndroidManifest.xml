<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RaktaVahini"
        android:usesCleartextTraffic="true">

        <!-- ✅ LOGIN SCREEN FIRST -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- Main Activity (no launcher now) -->
        <activity
            android:name=".MainActivity"
            android:exported="true" />

        <!-- Register Donor -->
        <activity
            android:name=".RegisterDonorActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <!-- Search Blood -->
        <activity
            android:name=".SearchBloodActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <!-- Donor Profile -->
        <activity
            android:name=".DonorProfileActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <!-- Donation Log -->
        <activity
            android:name=".DonationLogActivity"
            android:exported="false"
            android:parentActivityName=".DonorProfileActivity" />

    </application>

</manifest>